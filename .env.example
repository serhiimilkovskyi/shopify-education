# Storefront API — used in theme (browser), synced to snippets/ by npm run sync-env
SHOPIFY_STOREFRONT_ACCESS_TOKEN=
SHOPIFY_STOREFRONT_API_URL=/api/2024-10/graphql.json

# Admin API — server-side only, NEVER put in theme/liquid files
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
