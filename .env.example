# Storefront API — used in theme (browser), synced to snippets/customer-storefront-config.liquid
SHOPIFY_STOREFRONT_ACCESS_TOKEN=

# Admin API — server-side only, NEVER put in theme/liquid files
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
